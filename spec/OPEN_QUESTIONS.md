# Open questions, proposed defaults and missing inputs

Reconciled against `SOCCERMASTER_RPG_STANDALONE_V1_1.md` (the spec). Each entry is resolved by the
owner, never by consulting an earlier reference. "Default" is what the implementation uses until
the owner decides; it is marked provisional in-game or in code where visible.

Status legend: **Blocking** — cannot implement without it · **Default in use** — implemented with
the stated proposal · **Deferred** — outside the first playable milestone.

| # | Spec § | Item | Proposed default | Status |
|---|---|---|---|---|
| 1 | 1 | The standalone specification. | Supplied as v1.1 and saved to `spec/`. | Resolved |
| 2 | 22 | Technology stack. | TypeScript + Vite + Vitest, Canvas 2D, localStorage saves, static Cloudflare Pages build (`npm run build` → `dist`). See `docs/IMPLEMENTATION_PLAN.md` §1. | Default in use |
| 3 | 5 | Names of the best friend, parents, teammates, rivals; friend's position; cast biographies. | Placeholder cast with clearly fictional names, marked `reviewStatus: "proposal"` in `content/story/cast.json`. Best friend defaults to a midfielder (8); FC Batavia carries 10 players for 9 shirts, so the user and the friend always start and one named teammate sits. If the user also picks 8, the friend moves to the nearest shirt (6) — `squadFor` in `src/roster/roster.ts`. | Default in use |
| 4 | 3 | Player-creation fields (name, appearance options, birth month for age-group eligibility, dominant foot). | Name, appearance preset, dominant foot, birth month. | Default in use |
| 5 | 3 | Authored differences between boys' and girls' campaigns. | Different friend, different opening kick-about scene, different family situation and school social thread; identical soccer rules and attribute ranges. Content is a proposal. | Default in use |
| 6 | 15 | Age/competition rules for U11 9v9 (field size, ball size, heading, offside, build-out line, GK punts, match length, substitutions) verified from current primary sources. | A `rules/u11-9v9.json` profile with each value tagged `source: "unverified"` until citations are added. Offside on; heading disallowed; build-out line on; 2 × 30 min. | Default in use — needs verification |
| 7 | 3, 15 | Match formats for U12–U16 (9v9 vs 11v11, field, duration). | Not implemented. Later seasons are blocked on an explicit ruleset per age group. | Deferred / Blocking for seasons 2–6 |
| 8 | 9, 12 | Slow-motion time scale, decision-window length, extension for accessible input, timeout behaviour. | Resolved by the owner (2026-09, match-system audit approval): official-match moments freeze at the decision point; the answer period is **15 seconds of real time**, starting only after the freeze and after the question, answers and any accessibility read-aloud are complete; pause stops it; the user may answer early. Timeout records `timeout` / no decision with **no user decision grade**; the engine selects the character's action so the match continues and that action is graded separately and shown as the character acting because the player did not choose; a single timeout is never an excessive punishment (the story may remember repeated hesitation). Implemented in the cinematic decision-match PR (PR B). The pre-PR-B runtime still uses the 0.3 slow-motion window described in `docs/IMPLEMENTATION_PLAN.md` §3.1. | Resolved — implementation in PR B |
| 9 | 13 | Weights of match evidence vs training evidence in soccer assessment. | Match 0.7 / training 0.3 for tactical understanding; technical development weights training practice higher (0.5/0.5). | Default in use — needs review |
| 10 | 14 | Per-role moment targets (GK, defenders). | Resolved by the owner: **12–18 meaningful direct-involvement moments** per completed match for every position (hard player-facing range). Outfield moments are primarily first-touch decisions. Goalkeeper moments are direct involvements — backpass, distribution, playing through pressure, claim / punch / hold, starting position, 1v1 positioning, sweeping, cross management, rebound control, communication that determines the next keeper action — and count even when the best decision is positioning or delaying; unrealistic keeper touches are never fabricated. The pre-PR-B runtime still runs `DEFAULT_PACING` / `GK_PACING` (18–25 total) and reports shortfalls through `coverageReport`; PR B re-baselines moment selection to 12–18. | Resolved — re-baseline in PR B |
| 11 | 16 | The full tactical catalog (≥ 40 per position, 360 minimum). | Prototype ships 63 provisional entries (5–7 per outfield role, 13 for the keeper) generated from `tools/authorCatalog.ts` into `content/catalog/provisional-u11.json`, every one `review.status: "provisional"`. Restart-phase situations (corners, free kicks, throw-ins as tactical moments) are not authored: recognition runs in open play only, except the taker's own delivery. | Blocking for full game; Default in use for prototype |
| 12 | 17 | Numerical thresholds for state-championship qualification; tournament invitation thresholds. | Top 50 % of league table at cutoff qualifies; invitations: any team eligible for Valley Rising Cup and Presidential Cup, ≥ .500 record for Tuzona Challenge, Royal Holiday Classic and Desert Rush Invitational; Pacific Wave Cup and Lone Star International Cup require U13+. | Default in use |
| 13 | 17 | Registration cutoff dates and league season dates. | Fall league Sep–Nov, spring league Feb–Apr, each home-and-away over ten Saturdays with two reserve Saturdays at the end of the term; state cutoff 1 Apr; tournament cutoffs 4 weeks before the event; the club registers in the 14 days before a cutoff if eligible on that day (`entryWindowDays` in `content/rules/competitions-u11.json`). Fixtures on a day that has passed unplayed receive a deterministic modelled result (`src/calendar/results.ts`). | Default in use |
| 14 | 18 | Fictional names for the five other clubs. | Placeholder names in `content/story/clubs.json`, marked proposal. | Default in use |
| 15 | 7 | Calendar slot model: slot count per day, activity durations, attendance rules, phone restrictions. | 4 slots/day (morning, school, afternoon, evening) on weekdays; 3 on weekends. Training Tue/Thu/Fri afternoons; league match Sat. Phone unavailable during the school slot. Missed mandatory training → attendance record + coach follow-up scene. | Default in use |
| 16 | 8 | Approved real demonstration videos for home assignments. | No links shipped. Assignment references a placeholder "approved demonstration" slot the owner fills. | Blocking for that content; flow implemented |
| 17 | 21 | Measurable mobile performance targets and representative phones. | Targets and budgets in `docs/PERFORMANCE.md` (60 fps typical / 30 fps floor, frame p95 ≤ 33 ms, sim and render p95 ≤ 4 ms each, ≤ 600 draw calls). `npm run perf` checks the CPU budget headlessly on every change; `?perf` shows the same summary in-app. Representative phones proposed: 2019–2020 mid-range Android and iPhone 11 class. Owner to confirm devices. | Default in use — headless budget met; on-device evidence not yet collected |
| 18 | 21 | Art assets (character art, environments, audio). | Original procedural art in the navy-and-cyan palette (`src/render/figures.ts`): lit turf with mown stripes, side-view sunset / treeline scenes, kit-coloured figures with shadows and hair/skin variation, a lit ball with height, trails and a shrinking shadow, glass-panel UI with screen transitions. This is as far as code alone takes the cinematic direction; hand-drawn character portraits, environment paintings and audio need an artist / asset pipeline and an owner brief. No audio. | Default in use — art/audio brief needed |
| 19 | 20 | AI dialogue provider, hosting and budget. | Interface only; disabled; authored fallback always present. | Deferred |
| 20 | 19 | Explicit unlock requirements for progression milestones. | `content/rules/progression-u11.json`: seven tracks (technical, tactical, physical, wellbeing, school, responsibility, pathway) with the sources that move each, and five unlocks, each stating what it grants (a conversation, an activity, support or an opportunity — never an attribute) and its thresholds: park sessions (friend ≥ 20), parent trust (parent ≥ 20, school ≥ 65), coach film-room feedback (coach ≥ 25, responsibility ≥ 45), captain conversation (tactical ≥ 45, responsibility ≥ 60), tryout invitations (pathway ≥ 30). The hub shows every requirement with the player's current value; an authored `unlock.*` scene plays once when a rule opens. Thresholds are proposals. | Default in use — thresholds need owner review |
| 21 | 6 | Editorial mix (30/15/55) measurement method. | Scenes tagged `tone: reward|adversity|everyday`; `npm run story:tones` reports the share per campaign and layer and fails if the U11 arc drifts > 15 points from target or lacks a tone. Current arc: 26 / 13 / 61 (6 / 3 / 14 of 23 scenes). Never affects results. | Default in use |
| 22 | 24 | Coaching review of provisional tactical content. | Not a software task; tracked as a review gate on each catalog file. | Blocking for "approved" label |
| 24 | 8 | Small-sided training design: which decisions each of 1v1, 2v2, 3v2 teaches, reps per session, grading bands. | Concepts space / pressure / support / timing / transition; 1v1 (carry, hold, feint vs one defender), 2v2 (pass, carry, hold with a supporting teammate vs two), 3v2 (exploit numbers up, overload timing, switch). Five reps per session, options carry/pass/shoot/hold; decision graded against the seeded best option, execution graded from draw/tap accuracy, outcome recorded separately (`src/training/smallSided.ts`). Tue/Thu/Fri rotation shifts each week. Content is provisional pending coaching review. | Default in use — needs coaching review |
| 25 | 7 | Fatigue model: how training, matches and rest interact; when tiredness affects play. | Integer 0–10 fact `fatigue`: training +2, match +3, home practice +1, rest −3, sleep −1/day. ≥ 7 is "tired": the hub says so and training decision windows shrink to 70 %. Rest and a friend activity are therefore real alternatives to training. | Default in use |
| 26 | 7, 17 | Preseason friendlies and their status. | One friendly per Saturday between joining and the first league fixture, generated deterministically; playable and reported like any match, but excluded from standings and tournament eligibility (`kind: "friendly"`). | Default in use |
| 27 | 5, 7 | Optional friend activity. | Crossbar challenge: five attempts each, friend shots seeded; records relationship + wellbeing evidence only, never soccer-training evidence; once per day; phone-gated (not offered in the school slot). | Default in use |
| 28 | 17 | Tournament weekend shape: games per entry, day split, clash rule, entry count. | Three games per entry: two on Saturday (morning, afternoon), one on Sunday morning; opponents drawn deterministically from the league clubs and four tournament-only guest clubs. A league round on the tournament Saturday moves to the term's first free reserve Saturday, keeping its original date (`movedFromDay`). The club enters every event it is eligible for at the cutoff (three open U11 cups, two `.500` cups, the state event on qualification). Tournament results never enter a league table. | Default in use |
| 29 | 5, 7 | Family attendance at tournaments. | When the club enters, a parent asks: go with the team, ride with the friend's family, or stay home. Staying home takes the club's games off the player's calendar (they are modelled, no missed-match talk) and costs the friend relationship a little later; the choice is per event. Scenes in `content/story/season-u11.json` are proposals (hotel common room, shade between games, medal ceremony). | Default in use — content needs review |
| 30 | 7 | "Let the days pass" from the hub. | Free slots pass without the rest bonus and school is attended on the way; stops at the next training, match, scene or after 40 slots. | Default in use |
| 31 | 17 | Off-screen scoreline model. | Seeded Poisson-style goals: base 2.1 per side, home edge 1.08, club-quality slope 1.65, capped at 9 (`src/calendar/results.ts`). Guest club names and qualities (`guest: true` in `content/story/clubs.json`) are placeholders. No calibration data yet. | Default in use — needs calibration data |
| 32 | 6 | The U11 season arc: central question, milestones, optional interactions, boys'/girls' differences. | `content/story/arc-u11.json`, all `proposal`. Central question quoted from the spec; twelve milestone scenes queued at most one per week when their evidence holds (first league match → "is this a real team?"; the striker's sibling rumour; a run or a slump from the player's own result streak; the fall table; the striker's trial and decision; the newcomer; the coach's winter question; the holiday car ride; the keeper; spring tryout talk); five optional scenes offered from the hub at the lunch table, in the family car and at the park with a 7-day cooldown; five unlock scenes. Boys' and girls' campaigns share the arc with authored differences in the keeper and holiday-car scenes and different teammates behind the striker/organiser/keeper/newcomer roles. Story never sets results, grades, positions or attributes; every choice's effects are reducer data. Tournament-hotel scenes remain in `season-u11.json`. | Default in use — content needs review |
| 33 | 6, 19 | Park sessions as a playable activity. | Once per day, unlocked by the friend relationship; weekday afternoons and weekends, weekday evenings only with parent trust. Runs the first-touch drill with the friend leading; records verified `park_first_touch` reps, technical/tactical evidence like the Thursday visit, and touch/read facts that only the friend knows (the coach never sees them). | Default in use |
| 34 | 20 | Consequence repair window. | Arc repairs are offered from the hub for the days stated on the choice (`withinDays`, typically 3) and vanish afterwards; the delayed consequence they cancel lands on its own day unless the world moved on (`unless`). | Default in use |
| 35 | 7, 19 | Season-end tryouts: date, destination clubs, roster capacity, evidence gates. | `content/rules/tryouts-u11.json`, all `proposal`. Tryouts on the authored Saturday 15 May 2027 (`competitions-u11.json`), the Monday after the season review opens the week. Destinations are the five non-guest league clubs from `clubs.json` with placeholder names and attractions (Sonoran United, Camelback Athletic, Mesa Verde SC, Saguaro Rangers, Red Rock FC); each U12 roster has an explicit capacity (14–16) and reserved places for returning players, so 1–4 places are open and `joinRoster` refuses a full roster. Two sessions on the day, each a real 1v1/2v2/3v2 drill at one club; strong reads / clean executions from that session are the only session evidence. Invitations (two, best clubs first) go out when the pathway unlock is reached and each club's `invitesWhen` holds; offers the day after tryouts need every `requires` line (attendance, matches, tracks, coach relationship, session evidence) and an open place; decision window seven days, then offers expire and the player is unattached unless they accepted or stayed. | Default in use — names, thresholds and capacities need owner review |
| 36 | 19, 20 | Recruiter promises vs offers. | A recruiter's promise is recorded as a `promise` when the invitation arrives, never as an offer. Its semantics are stated in the rule (`keptBy`): `offer` (Sonoran: "There's a place in our U12 squad for you if you come to the session") is delivered by an actual offer and broken by its absence; `next_season` (Camelback's weekly individual technical hour, Mesa Verde's "nobody sits for a whole half") stays open into next season whether or not the club offers — it can only be kept or broken by next season's world. Broken promises play `tryouts.promise_broken` and stay visible on the hub. | Default in use — semantics need owner review |
| 37 | 6, 19 | The friend's next club. | With the "together" stance the friend follows the player to the destination if a place remains, otherwise re-signs at FC Batavia; the relationship value, knowledge and cast identity never change with club membership, and `tryouts.friend_with` / `tryouts.friend_apart` play from the outcome. The striker who took the Sonoran trial in the arc is placed at Sonoran (taking one of its places) when the fact holds. | Default in use — content needs review |
| 38 | 7, 8 | Rondo and transition activities; juggling thresholds. | `rondo` (4v2 keep-ball, concepts support / pressure / timing) and `transition` (2v2, the ball has just been won, concepts transition / space) join the programme so the three weekly trainings rotate through five activities. Scenarios and scoring are proposals: the rondo's best-clean policy succeeds ~93% headless, poor reads fail. Juggling: flights 700–1050 ms, bands 90 / 190 / 320 ms of error (perfect / good / loose), a loose touch adds drift that narrows the window ~12% and shortens the next flight, three runs a session, a run stops at 50, milestone moments at 10 / 25 / 50, +1 technical for a best of five or more, +1 wellbeing for a new record. All numbers untested with real U11 players. | Default in use — coaching review |
| 39 | 7, 18, 19 | Hobbies: which, and what they do. | `content/rules/hobbies-u11.json`, all `proposal`: drawing, reading, guitar, video games, picked in-game from any free afternoon / evening (weekend mornings too), one session a day. Each session is a rest of sorts (fatigue −1; games 0) and +1 wellbeing; reading gives +1 school every second night, games −1 school every third; 3rd / 7th sessions play an authored moment (the games one sets up a screen-time promise with a delayed school cost and a repair). Hobbies never touch soccer attributes or verified evidence. Switching restarts the hobby's own count but keeps the lifetime total. | Default in use — owner review |
| 40 | 9, 12 | Real match duration budget. | Resolved by the owner: preferred **5–7 real minutes**, acceptable 4–8, hard maximum 8 under normal settings; no artificial waiting, longer cinematics or feedback screens to reach a length; report median / min / max across benchmark matches. `src/match/pace.ts` holds the bands (`PREFERRED_BAND_MS` 5–7, `ACCEPTABLE_BAND_MS` 4–8); `npm run pace -- 3 all all` reports min / median / max per role and player model. | Resolved |
| 41 | 21, 24 | Loading / freeze recovery. | Playtest reports of screens failing to load or pausing could not be reproduced in the browser harness (`pwtools/freezeHunt`, full match + every activity, no long tasks / errors); the one confirmed error was a negative canvas ellipse radius on a high ball (fixed), and two authored `queue_scene` links pointed at scenes that did not exist (`u11.trial_review`, `u11.school_check_in` — now authored and guarded by `tests/story/links.test.ts`). Defensive layer added: a global error screen (`src/app/recovery.ts`) with "Back to start" (save kept) and "Reload" (drops the offline cache), an 8 s boot watchdog in `index.html` with a cache-clearing retry, and the service worker no longer caches a failed shell. If a freeze recurs, the recovery screen's message (or the device / browser / step) is what is needed to reproduce it. | Default in use — needs a reproducible report |
| 42 | v2 §4, §6 | World Cup Knockout rules: players, clock, strikes, variants, escalation. | `src/minigame/worldCupKnockout.ts`, proposal: 6–10 players, one ball each per round (the player first, then the others), 4.2 s clock (`short_clock` 2.8 s), three strikes and out, first touch graded on direction away from the seeded pressure side, strike graded on aiming away from the blocker inside the "ball set" window (350–1000 ms after the touch), a far first touch capped at acceptable, a missed touch taken for you (weak). Variants `classic`, `tight_space`, `shadow_defender`, `short_clock`, `weak_foot`. Winning against a recess player with skill ≥ 0.7 adds competitive tension. Recess skill is authored per person (the rival 0.82, club players 0.55–0.75, classmates 0.3–0.55), never read from match attributes. Six-minute bell. | Default in use — needs owner playtest |
| 43 | v2 §5, §8 | Group Presentation topics and grading for U11–U12. | `content/school/presentations-u11.json` (`fractions_pizza`, `water_cycle`, `continents`, `story_sequence`, all `proposal`): three sections, five visual cards with one correct order, cue cards with one right option; accuracy from correct cards and cues, clarity from handoffs taken on time, teamwork from section fit, everyone speaking and not taking two sections yourself; a frozen partner is rescued (quietly), taken over (dominates) or waited out. Whether these read as fourth/fifth-grade classwork needs a teacher's eye. | Default in use — needs owner review |
| 44 | v2 §3, §7 | Episode One cast and calendar: the rival's club, the teacher, classmates, school-week shape. | `content/story/cast.json`, `episode1-u11.json`, all `proposal`: the rival is Sonoran United's number 10 and sits two desks over (Dario Quintero / Priya Sethi), teacher Ms. Ferreira, five named classmates from other clubs and no club; the episode runs Monday homeroom → recess → Tuesday training lesson → Wednesday presentation → home decision → Saturday match → Monday, queued by `src/story/episodes.ts` from the first full school week after joining. Episode Two is a hook only (`ep2_hook`). | Default in use — content needs review |
| 45 | v2 §3 | Relationship memory dimensions, decay and how they show in dialogue. | `src/story/memory.ts`: six dimensions (trust, respect, loyalty, jealousy, dependence, competitive tension) clamped to −20..20 and independent of the older single `relationship` value, which stays as the unlock/tryout input; remembered events carry observer, belief, confidence, visibility and a decay rule (`fades` after 28 days, `until_repaired`, `permanent`). Only the rival's and teacher's memories are read by Episode One text so far. | Default in use — needs owner review |
| 46 | 9, 11, 22 | Whether drawing / gesture controls survive anywhere once official matches are answer-only. | Default: they remain for training drills and minigames outside official matches (`src/gesture/`, small-sided screens, first-touch drill), where they teach technique and never decide match results; official-match moments use answer selection only (spec §11). | Default in use — owner review |
| 47 | 10, 16 | Answer order in an official-match moment. | Default: deterministic seeded shuffle per moment, never best-first and never the catalog authoring order, so the position of an answer carries no information about the engine's ranking. | Default in use — owner review |
| 48 | 15, 16 | Which engine-best options a role's answers may deliberately omit. | `DOCUMENTED_EXCLUSIONS` in `src/tactics/exclusions.ts`; the only entry is the goalkeeper carry (keepers distribute; the engine penalises a keeper carry −0.8). `tests/tactics/answerSet.test.ts` fails on any other omission of the engine's highest-scoring on-ball option across seeded matches for all nine roles. Adding an exclusion requires a soccer reason in that table. | Default in use — coaching review |
| 49 | 15 | Second-9 thresholds (`SECOND_NINE` in `src/sim/ai.ts`). | Ball ≥ 15% of the width past the centre line on the far flank; carrier pressure < 0.5 and progress ≥ 0.55; a teammate on the winger's flank ≥ 18% of the width from the centre and past halfway; striker within 10 m of the last defender line and central; far-post space ≥ 0.45 at the narrowing target (12% of the width from the centre, 1.5 m in front of the line); ≥ 3 outfield teammates behind the ball; back line more than 6 m off the goal line. Over seeds 11–14 the read is on for roughly 0.1–0.4% of controlled ticks and never for both wingers of a side at once (`tests/tactics/secondNine.test.ts`). Whether it should fire more often is a coaching call. | Default in use — coaching review |
| 23 | 10, 11 | Reference match statistics for U11 9v9 (passes, completion %, shots, turnovers per 60 min) to calibrate the simulation. | Provisional balance from `npm run sim:headless` (AI vs AI, seeds 1–10): ~1550 passes at ~80% completion, 6–15 shots, 0–4 goals per side, ~550 possession changes, 2–11 offsides. Pass volume is believed to be roughly double a real youth match; no direction bias measured. Constants in `src/sim/actions.ts`, `src/sim/ai.ts`, `src/sim/engine.ts` are tunable. | Default in use — needs calibration data |
